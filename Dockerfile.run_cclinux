ARG DOC_REG
FROM ${DOC_REG}/run_funos:latest
RUN apt-get update && \
	DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends iproute2 redis-server python-pip python-thrift

RUN pip install --upgrade-strategy only-if-needed --only-binary :all: flask redis 
